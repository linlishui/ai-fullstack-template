# 提示词：生成 OpenSpec

请基于 `requirements/requirement.md` 和现有 `openspec/` 目录内容，生成或更新 OpenSpec 风格规格文档。

要求：

- 不跳过需求分析
- 规格必须覆盖需求、设计、任务
- 明确模块边界、接口、数据模型、权限规则、异常场景
- 如有必要，在 `openspec/changes/` 中补充变更记录
- 输出你新增或更新了哪些 OpenSpec 文件
